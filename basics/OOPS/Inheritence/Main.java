package OOPS.Inheritence;

/*
 * SINGLE , MULTI , HIERARCHICAL, HYBRID AND 	MULTIPLE(only by interfaces) 
 * 
 * also, if we inherit parents then only no-arg constructors will get called directly
 * 
 * for arg constructor we need to use Super (keyword)  -> (Child1 ->impleted there)
*/
public class Main {

	public static void main(String[] args) {
		//Parent hello=new Parent();
		//Parent hell=new Parent("tarun");
		Child1 ch=new Child1();		//--> single level inheritance
		
			// Child2 ch2=new Child2();  //-> hiehrarchical
		
	}

}
